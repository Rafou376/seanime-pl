{
    "type": "manga-provider",
    "language": "typescript",
    "id": "hentai-origines",
    "name": "Hentai Origines (18+)",
    "author": "Rafou376",
    "version": "1.0.0",
    "template": "origines",
    "lang": "fr",
    "description": "Hentai Origines est une plateforme française de scantrad pour adultes",
    "manifestURI": "https://raw.githubusercontent.com/rafou376/seanime-pl/main/src/manga/hentai-origines/manifest.json",
    "icon": "https://raw.githubusercontent.com/rafou376/seanime-pl/main/src/manga/hentai-origines/icon.ico",
    "payload": "/// <reference path=\"./_shared/manga/manga-provider.d.ts\" />\nvar ANY_TAG = \"[a-zA-Z][a-zA-Z0-9]*\";\nvar ATTR_VALUE = `[\"']([^\"']*)[\"']`;\nvar VOID_TAGS = /* @__PURE__ */ new Set([\"area\", \"base\", \"br\", \"col\", \"embed\", \"hr\", \"img\", \"input\", \"link\", \"meta\", \"source\", \"track\", \"wbr\"]);\nfunction escapeRegExp(value) {\n  return value.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\");\n}\nfunction openTagPattern(className, tag) {\n  return `<(${tag})\\\\b[^>]*\\\\bclass=[\"'][^\"']*\\\\b${escapeRegExp(className)}\\\\b[^\"']*[\"'][^>]*>`;\n}\nfunction isVoidMatch(tagName, openTag) {\n  return VOID_TAGS.has(tagName.toLowerCase()) || /\\/>\\s*$/.test(openTag);\n}\nfunction classTokens(openTag) {\n  const value = openTag.match(/\\bclass=[\"']([^\"']*)[\"']/i)?.[1] ?? \"\";\n  return value.split(/\\s+/).filter(Boolean);\n}\nfunction findOpenTagByClassPrefix(html, prefix, tag) {\n  const re = new RegExp(`<(${tag})\\\\b[^>]*>`, \"gi\");\n  let match;\n  while ((match = re.exec(html)) !== null) {\n    if (classTokens(match[0]).some((token) => token.startsWith(prefix))) return match;\n  }\n  return null;\n}\nfunction findMatchingClose(html, tagName, fromIndex) {\n  const openRe = new RegExp(`<${tagName}\\\\b`, \"gi\");\n  const closeRe = new RegExp(`</${tagName}\\\\s*>`, \"gi\");\n  let depth = 1;\n  let cursor = fromIndex;\n  while (depth > 0) {\n    openRe.lastIndex = cursor;\n    closeRe.lastIndex = cursor;\n    const nextOpen = openRe.exec(html);\n    const nextClose = closeRe.exec(html);\n    if (!nextClose) return null;\n    if (nextOpen && nextOpen.index < nextClose.index) {\n      depth++;\n      cursor = nextOpen.index + nextOpen[0].length;\n    } else {\n      depth--;\n      if (depth === 0) return nextClose;\n      cursor = nextClose.index + nextClose[0].length;\n    }\n  }\n  return null;\n}\nfunction elementBody(html, match) {\n  const tagName = match[1];\n  if (isVoidMatch(tagName, match[0])) return null;\n  const startIndex = match.index + match[0].length;\n  const close = findMatchingClose(html, tagName, startIndex);\n  return close ? html.slice(startIndex, close.index) : null;\n}\nfunction isLeafMatch(html, match, prefix, tag) {\n  const body = elementBody(html, match);\n  if (body === null) return true;\n  return findOpenTagByClassPrefix(body, prefix, tag) === null;\n}\nfunction extractElementText(html, match) {\n  const body = elementBody(html, match);\n  return body === null ? null : stripTags(body) || null;\n}\nfunction getAttr(html, targetAttr, match, tag = ANY_TAG) {\n  const value = escapeRegExp(match.value);\n  const matchPattern = match.exact ? `${match.attr}=[\"']${value}[\"']` : `${match.attr}=[\"'][^\"']*\\\\b${value}\\\\b[^\"']*[\"']`;\n  const targetPattern = `${targetAttr}=${ATTR_VALUE}`;\n  const forward = new RegExp(`<${tag}\\\\b[^>]*\\\\b${matchPattern}[^>]*\\\\b${targetPattern}`, \"i\");\n  const backward = new RegExp(`<${tag}\\\\b[^>]*\\\\b${targetPattern}[^>]*\\\\b${matchPattern}`, \"i\");\n  return html.match(forward)?.[1] ?? html.match(backward)?.[1] ?? null;\n}\nfunction getAttrByClass(html, className, targetAttr, tag) {\n  return getAttr(html, targetAttr, { attr: \"class\", value: className }, tag);\n}\nfunction getLinkHrefByClass(html, className, tag = \"a\") {\n  return getAttrByClass(html, className, \"href\", tag);\n}\nfunction getTextByClassPrefix(html, prefix, tag = ANY_TAG) {\n  const re = new RegExp(`<(${tag})\\\\b[^>]*>`, \"gi\");\n  let match;\n  while ((match = re.exec(html)) !== null) {\n    if (!classTokens(match[0]).some((token) => token.startsWith(prefix))) continue;\n    if (!isLeafMatch(html, match, prefix, tag)) continue;\n    const text = extractElementText(html, match);\n    if (text) return text;\n  }\n  return null;\n}\nfunction getBlocksByClass(html, className, tag = ANY_TAG) {\n  const re = new RegExp(openTagPattern(className, tag), \"gi\");\n  const blocks = [];\n  let match;\n  while ((match = re.exec(html)) !== null) {\n    const tagName = match[1];\n    if (isVoidMatch(tagName, match[0])) {\n      blocks.push(match[0]);\n      re.lastIndex = match.index + match[0].length;\n      continue;\n    }\n    const startIndex = match.index + match[0].length;\n    const close = findMatchingClose(html, tagName, startIndex);\n    if (!close) continue;\n    const endIndex = close.index + close[0].length;\n    blocks.push(html.slice(match.index, endIndex));\n    re.lastIndex = endIndex;\n  }\n  return blocks;\n}\nfunction getImageUrl(html, tag = \"img\") {\n  const match = html.match(new RegExp(`<${tag}\\\\b[^>]*\\\\bdata-src=[\"']([^\"']+)[\"']`, \"i\")) ?? html.match(new RegExp(`<${tag}\\\\b[^>]*\\\\bsrc=[\"']([^\"']+)[\"']`, \"i\"));\n  return match?.[1] ?? null;\n}\nfunction stripTags(html) {\n  return html.replace(/<[^>]+>/g, \" \").replace(/&amp;/g, \"&\").replace(/&nbsp;/g, \" \").replace(/\\s+/g, \" \").trim();\n}\n\nvar CHAPTER_DATE_RE = /(\\d{1,2})\\s+(\\p{L}+)\\.?(?:\\s+(\\d{4}))?/u;\nvar MONTHS = [\n  [\"jan\"],\n  [\"fev\", \"f\\xE9v\"],\n  [\"mar\"],\n  [\"avr\"],\n  [\"mai\"],\n  [\"juin\"],\n  [\"juil\"],\n  [\"ao\"],\n  [\"sep\"],\n  [\"oct\"],\n  [\"nov\"],\n  [\"dec\", \"d\\xE9c\"]\n];\nvar Origines = class {\n  constructor() {\n    this.legacyMangaPaths = [];\n  }\n  getSettings() {\n    return {};\n  }\n  async search(opts) {\n    const body = `action=madara_child_search&term=${encodeURIComponent(opts.query)}`;\n    const res = await this.postForm(`${this.baseUrl}/wp-admin/admin-ajax.php`, body);\n    if (!res) return [];\n    const json = JSON.parse(await res.text());\n    const items = Array.isArray(json.data) ? json.data : [];\n    return items.filter((item) => item.title && item.url).map((item) => ({\n      id: this.toMangaSlug(item.url),\n      title: item.title,\n      image: item.thumb ? this.absUrl(item.thumb) : void 0\n    }));\n  }\n  async findChapters(id) {\n    const res = await fetch(`${this.baseUrl}/${this.mangaPath}/${id}/`);\n    if (!res.ok) return [];\n    return this.parseChapters(await res.text(), id);\n  }\n  async findChapterPages(id) {\n    const res = await fetch(`${this.baseUrl}/${this.mangaPath}/${id}/?style=list`);\n    if (!res.ok) return [];\n    return this.parsePages(await res.text());\n  }\n  async postForm(url, body) {\n    const res = await fetch(url, {\n      method: \"POST\",\n      headers: { \"Content-Type\": \"application/x-www-form-urlencoded\" },\n      body\n    });\n    return res.ok ? res : null;\n  }\n  get knownPaths() {\n    return /* @__PURE__ */ new Set([...this.legacyMangaPaths, this.mangaPath]);\n  }\n  splitSegments(path) {\n    return path.split(\"?\")[0].split(\"#\")[0].split(\"/\").filter((segment) => segment.length > 0);\n  }\n  pathSegments(path) {\n    const known = this.knownPaths;\n    const cleanPath = path.startsWith(\"http\") ? new URL(path).pathname : path;\n    return this.splitSegments(cleanPath).filter((segment) => !known.has(segment));\n  }\n  toMangaSlug(path) {\n    return this.pathSegments(path)[0] ?? path;\n  }\n  lastPathSegment(path) {\n    return this.splitSegments(path).pop();\n  }\n  absUrl(url) {\n    try {\n      return new URL(url, this.baseUrl).toString();\n    } catch {\n      return url;\n    }\n  }\n  parseChapters(html, mangaId) {\n    const chapters = [];\n    for (const block of getBlocksByClass(html, \"ori-chl-row\", \"div\")) {\n      const href = getLinkHrefByClass(block, \"ori-chl-corps\");\n      if (!href) continue;\n      const chapterSlug = this.lastPathSegment(href);\n      if (!chapterSlug) continue;\n      const id = `${mangaId}/${chapterSlug}`;\n      const name = getTextByClassPrefix(block, \"ori-chl-nom\") ?? getTextByClassPrefix(block, \"ori-chl\") ?? id;\n      const dateTitle = getAttrByClass(block, \"ori-chl-date\", \"title\", \"span\");\n      chapters.push({\n        id,\n        url: this.absUrl(`${this.mangaPath}/${id}/`),\n        title: name,\n        chapter: this.chapterNumber(name),\n        updatedAt: this.parseChapterDate(dateTitle)\n      });\n    }\n    return chapters.sort((a, b) => this.chapterOrderValue(a.chapter) - this.chapterOrderValue(b.chapter)).map((chapter, index) => ({ ...chapter, index }));\n  }\n  chapterOrderValue(chapter) {\n    const value = parseFloat(chapter);\n    return Number.isNaN(value) ? Number.POSITIVE_INFINITY : value;\n  }\n  parsePages(chapterHtml) {\n    const referer = `${this.baseUrl}/${this.mangaPath}/`;\n    return getBlocksByClass(chapterHtml, \"wp-manga-chapter-img\", \"img\").map((block) => getImageUrl(block)?.trim()).filter((url) => !!url).map((url, index) => ({\n      url: this.absUrl(url),\n      index,\n      headers: { Referer: referer }\n    }));\n  }\n  chapterNumber(name) {\n    return name.match(/(\\d+(?:\\.\\d+)?)/)?.[1] ?? name;\n  }\n  parseChapterDate(date) {\n    if (!date) return void 0;\n    const match = date.match(CHAPTER_DATE_RE);\n    if (!match) return void 0;\n    const [, day, monthLabel, year] = match;\n    const month = this.monthNumber(monthLabel);\n    if (month === void 0) return void 0;\n    const now = /* @__PURE__ */ new Date();\n    const resolvedYear = year ? parseInt(year, 10) : now.getFullYear();\n    let result = new Date(Date.UTC(resolvedYear, month, parseInt(day, 10)));\n    if (!year && result.getTime() > now.getTime()) {\n      result = new Date(Date.UTC(resolvedYear - 1, month, parseInt(day, 10)));\n    }\n    return result.toISOString();\n  }\n  monthNumber(month) {\n    const name = month.toLowerCase();\n    const index = MONTHS.findIndex((prefixes) => prefixes.some((prefix) => name.startsWith(prefix)));\n    return index === -1 ? void 0 : index;\n  }\n};\n\nvar Provider = class extends Origines {\n  constructor() {\n    super(...arguments);\n    this.baseUrl = \"https://hentai-origines.com\";\n    this.mangaPath = \"manga\";\n  }\n};\n"
}
